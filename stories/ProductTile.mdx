import { Canvas, Meta, Title, Controls, Stories } from '@storybook/blocks';
import * as ProductTileStories from './ProductTile.stories';

<Meta of={ProductTileStories} />
<Title />

## When to use

When you need to display pictures

There are 3 main types

- Products: Shows an image together with product description passed as props.
- Product: Shows an image without product description.
- Carousel: Shows multiple images in a carousel view together with product description passed as props.

<Stories />

## Props

<Controls />
