import { Canvas, Meta, Title, Controls, Stories } from '@storybook/blocks';
import * as IconContainerStories from './IconContainer.stories';

<Meta of={IconContainerStories} />

<Title />

## When to use

- When there are icons that have to displayed within a container

<Stories />

<Controls />
