import { Canvas, Meta, Title, Controls, Stories } from '@storybook/blocks';
import * as PageBannerStories from './PageBanner.stories';

<Meta of={PageBannerStories} />
<Title />

## When to use

When you want to show a hero image with an overlay, a text and and clickable action

There are 2 types

- Image: uses the img html tag.
- backgroundImage: uses the backgroundImage css option

<Stories />

## Props

<Controls />
